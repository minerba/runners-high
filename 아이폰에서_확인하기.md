# 아이폰에서 앱 확인하는 방법 📱

## 🌐 방법 1: 무료 웹 호스팅 배포 (추천!)

### 1단계: 웹 빌드 완료 기다리기
현재 `flutter build web` 명령어가 실행 중입니다. 완료될 때까지 기다려주세요.

### 2단계: GitHub에 업로드

```powershell
cd "c:\Users\user\OneDrive\바탕 화면\Runners_high"

# Git 초기화 (처음만)
git init
git add .
git commit -m "Initial commit"

# GitHub에서 새 저장소 생성 후
git remote add origin https://github.com/[username]/runners-high.git
git push -u origin main
```

### 3단계: Vercel로 배포 (가장 쉬움!)

1. [Vercel.com](https://vercel.com) 방문
2. GitHub 계정으로 로그인
3. "Import Project" 클릭
4. runners-high 저장소 선택
5. 빌드 설정:
   - **Framework Preset**: Other
   - **Build Command**: `flutter build web`
   - **Output Directory**: `build/web`
6. Deploy 클릭!

완료되면 `https://runners-high.vercel.app` 같은 URL이 생성됩니다.
이 URL을 **아이폰 Safari**에서 열면 앱을 볼 수 있어요! 🎉

---

## 🏠 방법 2: 같은 WiFi 연결 (PC와 아이폰이 같은 WiFi일 때)

### 1단계: PC IP 주소 확인
```powershell
ipconfig
# IPv4 주소 찾기 (예: 192.168.0.10)
```

### 2단계: 웹서버 모드로 실행
```powershell
cd "c:\Users\user\OneDrive\바탕 화면\Runners_high"
$env:Path += ";$env:USERPROFILE\flutter\bin"
flutter run -d web-server --web-hostname=0.0.0.0 --web-port=8080
```

### 3단계: 아이폰에서 접속
iPhone Safari에서:
```
http://192.168.0.10:8080
```
(192.168.0.10을 자신의 PC IP로 변경)

---

## 🔥 방법 3: Firebase Hosting (영구 무료)

### 1단계: Firebase CLI 설치
```powershell
npm install -g firebase-tools
```

### 2단계: Firebase 프로젝트 설정
```powershell
cd "c:\Users\user\OneDrive\바탕 화면\Runners_high"
firebase login
firebase init hosting

# Public directory: build/web
# Single-page app: Yes
```

### 3단계: 배포
```powershell
firebase deploy
```

완료되면 `https://your-project.web.app` URL 생성!

---

## 🌍 방법 4: Netlify (드래그앤드롭)

1. [Netlify.com](https://netlify.com) 로그인
2. "New site" → "Deploy manually"
3. `build/web` 폴더를 드래그앤드롭
4. 완료! URL 생성됨

---

## ✅ 가장 빠른 방법

**현재 상황**: 빌드가 완료되면 `build/web` 폴더가 생성됩니다.

**가장 빠른 순서**:
1. Netlify.com 접속
2. 회원가입/로그인 (무료)
3. "Sit" → "Add new site" → "Deploy manually"
4. `c:\Users\user\OneDrive\바탕 화면\Runners_high\build\web` 폴더를 웹사이트에 드래그앤드롭
5. 생성된 URL을 아이폰에서 접속!

**5분이면 끝!** 🚀

---

## 🎯 빌드 완료 후 해야 할 일

빌드가 끝나면:
```powershell
# 빌드 폴더 확인
cd "c:\Users\user\OneDrive\바탕 화면\Runners_high\build\web"
dir
```

`index.html` 파일이 보이면 성공입니다!
이 `build\web` 폴더 전체를 Netlify에 업로드하면 됩니다.
